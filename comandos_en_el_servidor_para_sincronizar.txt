#!/bin/bash

# Sincroniza con GitHub
cd /cloudclusters/ListpreciosWeb
git fetch --all
git reset --hard origin/master

# Mueve archivos de configuración
mv /cloudclusters/ListpreciosWeb/flask.conf /cloudclusters/config/nginx/conf.d/flask.conf
mv /cloudclusters/ListpreciosWeb/uwsgi.conf /cloudclusters/config/nginx/sites-enabled/uwsgi.conf
mv /cloudclusters/ListpreciosWeb/uwsgi.ini /cloudclusters/config/uwsgi.ini
# Reinicia Nginx
supervisorctl restart nginx

# Verifica estado de servicios
supervisorctl status